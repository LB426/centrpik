<h1>Тесты</h1>
<table class="table table-striped">
  <thead>
	<tr>
	  <th>#</th>
	  <th>Название теста</th>
	  <th>&nbsp;</th>
	  <th>&nbsp;</th>
	</tr>
  </thead>
  <tbody>
<% @tests.each do |test| %>
  <tr>
	<th scope="row"><%= test.id %></th>
	<td><%= test.name %></td>
	<td><a class="btn btn-primary btn-xs" href="#" role="button">Пройти тестирование</a></td>
	<% if current_user.admin? %>
	  <td>
		<%= link_to "Редактировать", test_edit_path(test), :class => "btn btn-danger btn-xs" %>
	  </td>
	<% end %>
  </tr>
<% end %>
  </tbody>
</table>
